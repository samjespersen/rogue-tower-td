import React from 'react';

const Enemy = () => {
    return (
        <div style={{ width: '7px', height: '7px', borderRadius: '100%', backgroundColor: 'red' }}></div>
    );
};

export default Enemy;
